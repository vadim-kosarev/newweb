»де€ в следующем -- сыпать в базу данных разнотипные данные из совершенно различных событий
- процесс запущен
- тест пройден/не пройден
- получено письмо 
и т.д.

Ёти данные должны сохран€тьс€ в Ѕƒ. интерфейс должен быть максимально упрощенным.
ƒолжна быть возможность легко строить запросы, раскрашенные таблицы по запросам и графики (какбе Excel).


===============================

“аблицы с префиксом sys_ -- системные, создаютс€ во врем€ установки ѕќ, не удал€ютс€
“аблицы с префиксом data_ -- хранение данных. ћогут удал€тьс€ дл€ очистки базы. —оздаютс€ автоматически во врем€ работы.


===============================
ƒобавление данных в базу данных:

genStaterAddEvent.cmd event=processStarted d_date_startDate=123338282828 \
    d_ext_host=BREEZE7 d_int_pid=99943 d_str_path=C:\WINDOWS\notepad.exe \
    tags=RTS,Test,Beta,3.04,PreRelease



event -- им€ евента. ƒл€ каждого нового уникального имени эвента создаетс€ отдельна€ таблица
 data_events_processStarted

d_date_startDate -- поле данных. ƒл€ каждого такого пол€ создаетс€ столбец в таблице (??? автоматом или при создании таблицы)
 d_ == данные
 _date_ = тип данных - ƒата
 _startDate = им€ данных

d_ext_host=BREEZE7 -- поле данных. 
 d_ == данные
 _ext_ == означает, что дл€ этих данных должна быть создана отдельна€ таблица (значени€ будут повтор€тьс€) и в исходную таблицу
  записываетс€ только ID (внешний ключ)
 _host == им€ данных. им€ таблицы будет data_ext_host и будет содержать два пол€ id, data. ѕри наличии строчки в таблице
   оттуда просто возьметс€ id, при отстутсвии в таблицу запишетс€ строка [ "1", "BREEZE7" ]

d_int_pid -- поле данных. INT, записываетс€ полем в data_events_processStarted

d_str_path -- поле данных. VARCHAR, записываетс€ полем в data_events_processStarted

---

tags -- список тегов дл€ записи... до 10 штук. «начени€ тегов хран€тс€ в таблице
  sys_tags ( аналогично _ext_ данным )
  в таблице data_events_processStarted создаетс€ 10 полей с именами tag0, tag1, tag2, ..., tag9

